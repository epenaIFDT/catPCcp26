# Herramienta de administración: solo debe existir en local, nunca subirse.
admin-local/

# Exportaciones locales (catálogo completo sin cifrar, con precios) —
# nunca deben subirse al repo público. Los scripts de ScriptsLocales/ sí
# quedan versionados (no son sensibles), solo su carpeta de resultados.
ScriptsLocales/Data/
